# Creator 工具实机清单（转码 / 竖屏 / GIF / 压片）

配合桌面 **v2.4.0**。打开侧栏 **快捷视频工具 (Creator)**，先导入一段 5–15 秒横屏 MP4。

引擎层已自动验证：convert / vertical(color+gradient) / GIF / compress 均可产出文件。  
下列为 **UI 路径** 勾选项。

---

## 0. 前置

- [ ] Settings 中 yt-dlp / ffmpeg 显示可用
- [ ] 准备：横屏短视频 +（可选）竖屏素材

---

## 1. 转码 convert

| 步骤 | 预期 | 结果 |
|------|------|------|
| 工具箱 → 格式转换 | 打开参数区 | |
| 目标 mp4 / 质量 low→high | 能选 | |
| 开始 → 选保存路径 | 进度走动，生成文件可播 | |
| （可选）转 mp3 | 仅音频，可播 | |

备注：无 NVIDIA 驱动时可能先尝试硬件编码再回落 CPU，属正常（已修正无效 NVENC preset）。

---

## 2. 竖屏 vertical（本轮重点修复）

| 步骤 | 预期 | 结果 |
|------|------|------|
| 工具箱 → 竖屏转换 | 打开参数区 + 预览安全区 | |
| **背景 = 模糊**，强度 20 | 输出 9:16，虚化背景 | |
| **背景 = 纯色**，选明显颜色如红 | **输出背景是该色**（以前会变成黑） | |
| **背景 = 渐变**，双色 | 上下渐变可见 | |
| 等比缩放 80% + 偏移 | 主体更小/偏移（以前参数被丢弃） | |
| 开始转换 | 生成可播 mp4 | |

---

## 3. GIF

| 步骤 | 预期 | 结果 |
|------|------|------|
| 工具箱 → GIF | 可选起止/宽度/帧率 | |
| 开始 | 生成 `.gif`，循环播放 | |
| 取消（若支持） | 可中断 | |

---

## 4. 压片 compress

| 步骤 | 预期 | 结果 |
|------|------|------|
| 工具箱 → 智能压缩 | 质量/编码/码率可选 | |
| 质量 low | 体积小于原片（一般） | |
| （可选）目标体积 | 接近目标 | |
| 导出 | 可播 | |

---

## 5. 其它（可选）

| 工具 | 快速过 |
|------|--------|
| 变速 1.5x / 0.5x | 时长变化、音频同步 |
| 水印 文字 | 画面可见 |
| 快速剪辑 一段 | 裁切正确 |
| 去音轨 | 无声可播 |

---

## Editor 导出（次优先抽查）

- [ ] 多轨时间线：加 1 视频轨短片
- [ ] 点导出 → 选 mp4 路径
- [ ] 成功 toast + 文件可播
- [ ] 无素材时导出应提示/禁用

---

## 本机自动结果（开发机 2026-07-17）

| 工具 | Handler 冒烟 |
|------|----------------|
| convert mp4 | ✅ |
| vertical color | ✅ |
| vertical gradient | ✅ |
| GIF | ✅ |
| compress h264 | ✅ |

代码修复摘要：

1. **竖屏**：UI 的 color/gradient/blur/scale/offset **真正传到 ffmpeg 滤镜**（此前只传了 style 名且 color→black）。
2. **VideoService.makeVertical**：透传完整 options。
3. **转码 NVENC preset**：`balanced` → `p1/p4/p5`，减少无效重试。
4. **GIF**：注册到 FFmpegRunner，便于取消。

---

## 签字

| 项 | 值 |
|----|-----|
| 日期 | |
| 测试者 | |
| Blockers | |
| 可发版 Creator 工具 | Yes / No |
